<h1>Add new Category</h1>
<%= form_for @category do |f| %>
  <%= render "shared/errors_messages", object: f.object %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <p></p>
  <%= f.submit %>
<% end %>
